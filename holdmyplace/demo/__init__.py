"""Member-facing demo, generated from the domain modules.

Nothing here decides anything. Every figure the demo displays (feasibility
verdicts, queue positions, fulfillment modes, arrival dates, costs, warning
copy) is computed by `holdmyplace.domain` and serialized for presentation.
The HTML is a rendering layer with no rules of its own, so the screens cannot drift away
from the system they illustrate.
"""
